
# Django 설정
SECRET_KEY=asd1004!!
DEBUG=False
ALLOWED_HOSTS=your-domain.com,*.run.app

# Redis 설정 (Memorystore)
REDIS_URL=redis://10.0.0.3:6379/0


# 보안 설정
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# CORS 설정
# 허용할 프론트엔드 도메인들 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-admin-domain.com
# CORS 자격 증명 허용 여부 (쿠키, 인증 헤더 등)
CORS_ALLOW_CREDENTIALS=true
# 모든 오리진 허용 (프로덕션에서는 false 권장)
CORS_ALLOW_ALL_ORIGINS=false

# CSRF 보안 설정
# CSRF 신뢰할 수 있는 오리진들 (쉼표로 구분)
CSRF_TRUSTED_ORIGINS=https://your-frontend-domain.com,https://your-admin-domain.com

# 백엔드 도메인 설정 (CORS 유틸리티에서 사용)
BACKEND_DOMAIN=https://your-backend-domain.run.app

# 로깅 설정
LOG_LEVEL=INFO
GOOGLE_CLOUD_PROJECT=dental-ai-2024

# API 키 (필요시)
# GOOGLE_MAPS_API_KEY=your-api-key
# NAVER_CLIENT_ID=your-client-id
# NAVER_CLIENT_SECRET=your-client-secret